{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node scripts/build-static.mjs",
        "check": "node --check public/assets/js/rg-premium.js && node scripts/check-site.mjs",
        "test": "npm run build && npm run check"
    }
}
